apiVersion: v1
kind: Service
metadata:
  name: supermario
  labels:
    app: supermario
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: supermario
